

import HomeClient from "./HomeClient";

export default function Home() {
  return (
    <main style={{ 
      width: '100vw',
      height: '100vh',
      overflow: 'hidden',
      padding: 0,
      margin: 0
    }}>
      <HomeClient />
    </main>
  );
}
